# Claude Code Instructions

Use `AGENTS.md` as the primary project instruction file.

Claude-specific notes:

- You are the primary coding agent for RIT.
- Work in a clean branch or Claude worktree based on latest `main`.
- For investigation-only tasks, a clean Claude worktree is acceptable.
- For coding tasks, report branch/worktree path clearly.
- Do not merge PRs.
- Do not push to `main` or `release`.
- Follow the issue scope and allowed/forbidden files.